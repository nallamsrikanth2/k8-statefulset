apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    name: frontend
    project: expense
spec:
  type: LoadBalancer
  selector:
    name: frontend
    project: expens
  ports:
  - protocol: TCP
    port: 80
    targetPort: {{ .Values.ConfigMap.port }}