apiVersion: v1
kind: Service
metadata:
 name: backend
 labels:
  name: backend
  project: expense
spec:
  selector:
    name: backend
    project: expense
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  
